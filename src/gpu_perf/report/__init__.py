"""Phase 4 — correlation + comparison reporting.

Reads RunRecord JSONs (real or SIMULATED), classifies each run's bottleneck by
correlating the timing breakdown with GPU telemetry, builds strong/weak scaling
comparisons (speedup + efficiency), and renders console/JSON/Markdown + charts.
"""
